#include <stdio.h>
/**
 * _strlen * dsa asdaweda sdad as
 *
 * @s: asd asd ad
 *
 * Return: ads ads
 */

int _strlen(char *s)
{
	int counter;

	for (counter = 0; *s != '\0'; s++)
		++counter;

	return (counter);
}
